apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: tech-challenge
  labels:
    app: tech-challenge
    component: api
spec:
  type: LoadBalancer
  selector:
    app: tech-challenge
    component: api
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
